# 1. 앱 기본 정보
spring.application.name=restok-ai-engine

# 2. 보안 인프라 (Zero-Exposure)
# API Key와 DB 정보는 환경 변수에서 주입받습니다.
spring.datasource.url=${RESTOK_DB_URL:jdbc:mariadb://localhost:3308/restok_db}
spring.datasource.username=${RESTOK_DB_USER:root}
spring.datasource.password=${RESTOK_DB_PASSWORD:1234}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# AI 인증 토큰 (Python 통신용)
restok.ai.secure-token=${RESTOK_AI_SECURE_TOKEN:restok-secret-2024}
restok.ai.engine-url=${RESTOK_AI_ENGINE_URL:http://localhost:8000/analyze-full}

# 3. JPA/Hibernate 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# 4. 서버 설정
server.port=8080